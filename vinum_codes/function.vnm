fun pinta check(pinta number) {
    pinta temp = 0
    pour pinta t = 1; t < number; t = t + 1 {
        temp = temp + t
    }
    refill temp
}

fun subfun() {
    toast("Void!")
}

fun kega sqr(kega num) {
    refill num * num
}

subfun()

toast(check(5))
kega num = sqr(10000)
toast(num)